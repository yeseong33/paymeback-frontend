# 송금 애니메이션 디자인 가이드

## 개요

순차 송금 플로우에서 사용되는 UI 컴포넌트와 애니메이션 정의 문서입니다.

---

## 1. 송금 카드 (Transfer Card)

### 레이아웃 구조

```
┌─────────────────────────────────────────┐
│  ←  송금 (1/3)                          │  ← 헤더
│  ━━━━━━━━━░░░░░░░░░░░░                  │  ← 진행 바
│                                         │
│      ┌───────────────────────┐          │
│      │ ████████████████████  │          │  ← 그라데이션 헤더
│      │        💸             │          │     (blue-500 → indigo-600)
│      │                       │          │
│      ├───────────────────────┤          │
│      │        [김]           │          │  ← 프로필 이니셜
│      │       김철수          │          │  ← 이름
│      │   토스뱅크 1234567    │          │  ← 계좌 정보
│      │                       │          │
│      │   ┌───────────────┐   │          │
│      │   │   송금 금액    │   │          │  ← 금액 박스
│      │   │   30,000원    │   │          │     (gray-50 배경)
│      │   └───────────────┘   │          │
│      │                       │          │
│      │ ┌───────────────────┐ │          │
│      │ │ 30,000원 송금하기  │ │          │  ← 송금 버튼
│      │ └───────────────────┘ │          │     (blue-500 → indigo-500)
│      └───────────────────────┘          │
│         └─────────────────┘             │  ← 뒤 카드 1
│            └──────────────┘             │  ← 뒤 카드 2
│                                         │
│         다음에 할게요 →                  │  ← 건너뛰기
└─────────────────────────────────────────┘
```

### 카드 스택 효과

| 카드 | transform | opacity | z-index |
|------|-----------|---------|---------|
| 현재 카드 | `scale(1)` | `1` | `20` |
| 뒤 카드 1 | `translateY(12px) scale(0.96)` | `0.75` | `9` |
| 뒤 카드 2 | `translateY(24px) scale(0.92)` | `0.50` | `8` |

### 색상 팔레트

| 요소 | Light Mode | Dark Mode |
|------|------------|-----------|
| 배경 | `gray-50` | `gray-900` |
| 카드 | `white` | `gray-800` |
| 헤더 그라데이션 | `blue-500 → indigo-600` | 동일 |
| 금액 박스 | `gray-50` | `gray-700/50` |
| 송금 버튼 | `blue-500 → indigo-500` | 동일 |
| 완료 버튼 | `green-500 → emerald-500` | 동일 |

---

## 2. 애니메이션 정의

### 2.1 카드 날아가기 (Card Fly Up)

송금 완료 시 현재 카드가 위로 날아가는 애니메이션

```css
@keyframes card-fly-up {
  0% {
    transform: translateY(0) scale(1);
    opacity: 1;
  }
  100% {
    transform: translateY(-120%) scale(0.9);
    opacity: 0;
  }
}

.animate-card-fly-up {
  animation: card-fly-up 0.5s ease-in-out forwards;
}
```

**사용 시점**:
- "송금 완료했어요" 버튼 클릭 후
- API 성공 응답 시

**시퀀스**:
```
[버튼 클릭] → [API 호출] → [성공] → [카드 날아감] → [0.5초 후 다음 카드 표시]
```

### 2.2 스케일 인 (Scale In)

완료 화면 등장 시 부드럽게 확대되며 나타나는 애니메이션

```css
@keyframes scale-in {
  0% {
    transform: scale(0.8);
    opacity: 0;
  }
  50% {
    transform: scale(1.05);  /* 살짝 오버슈트 */
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}

.animate-scale-in {
  animation: scale-in 0.5s ease-out forwards;
}
```

**사용 시점**:
- 모든 송금 완료 후 축하 화면 표시

### 2.3 바운스 인 (Bounce In)

강조 요소가 튀어나오는 애니메이션

```css
@keyframes bounce-in {
  0% {
    transform: scale(0);
  }
  50% {
    transform: scale(1.2);
  }
  70% {
    transform: scale(0.9);
  }
  100% {
    transform: scale(1);
  }
}

.animate-bounce-in {
  animation: bounce-in 0.5s ease-out forwards;
}
```

**사용 시점**:
- 체크 아이콘 표시
- 완료 배지 표시

### 2.4 콘페티 떨어지기 (Confetti Fall)

축하 화면에서 색종이가 떨어지는 애니메이션

```css
@keyframes confetti-fall {
  0% {
    transform: translateY(-100%) rotate(0deg);
    opacity: 1;
  }
  100% {
    transform: translateY(100vh) rotate(720deg);
    opacity: 0;
  }
}

.confetti {
  position: absolute;
  width: 10px;
  height: 10px;
  border-radius: 2px;
  animation: confetti-fall 3s ease-in-out forwards;
}
```

**콘페티 색상**:
```javascript
['#fff', '#FFE66D', '#4ECDC4', '#95E1D3', '#F8B500']
```

**배치**:
- 30개 생성
- 랜덤 X 위치 (`left: ${Math.random() * 100}%`)
- 랜덤 딜레이 (`animationDelay: ${Math.random() * 0.5}s`)

---

## 3. 송금 완료 화면

### 레이아웃

```
┌─────────────────────────────────────────┐
│                    ✕                    │  ← 닫기 버튼
│                                         │
│            🎊 🎊 🎊 🎊 🎊               │  ← 콘페티 애니메이션
│                                         │
│              ┌─────────┐                │
│              │   🎉    │                │  ← 파티 아이콘
│              └─────────┘                │     (animate-scale-in)
│                                         │
│               완료!                      │
│       모든 송금이 완료되었습니다         │
│                                         │
│           ┌─────────────┐               │
│           │      3      │               │  ← 총 건수
│           │  건 송금 완료 │               │
│           └─────────────┘               │
│                                         │
│         ┌─────────────────┐             │
│         │      확인       │             │  ← 확인 버튼
│         └─────────────────┘             │     (white, green 텍스트)
└─────────────────────────────────────────┘

배경: green-500 → emerald-600 그라데이션
```

### 색상

| 요소 | 색상 |
|------|------|
| 배경 그라데이션 | `from-green-500 to-emerald-600` |
| 아이콘 배경 | `white/20` (반투명) |
| 텍스트 | `white` |
| 서브 텍스트 | `white/80` |
| 건수 박스 | `white/20` backdrop-blur |
| 확인 버튼 | `white` 배경, `green-600` 텍스트 |

---

## 4. 버튼 상태 변화

### 송금 버튼 흐름

```
[토스 앱 열기 전]          [토스 앱 열고 돌아온 후]
┌─────────────────┐       ┌─────────────────┐
│ 💸 30,000원     │  →    │ ✓ 송금          │
│    송금하기     │       │   완료했어요     │
└─────────────────┘       └─────────────────┘
   blue gradient             green gradient
```

### 버튼 스타일

| 상태 | 배경 | 그림자 |
|------|------|--------|
| 송금하기 | `from-blue-500 to-indigo-500` | `shadow-blue-500/25` |
| 송금 완료했어요 | `from-green-500 to-emerald-500` | `shadow-green-500/25` |
| 로딩 | 동일 + `opacity-50` | 스피너 표시 |
| 비활성화 | `gray-300` | 없음 |

---

## 5. 진행 바 (Progress Bar)

### 구조

```
송금 (1/3)
━━━━━━━━━░░░░░░░░░░░░  33%
```

### 스타일

| 요소 | Light Mode | Dark Mode |
|------|------------|-----------|
| 배경 트랙 | `gray-200` | `gray-700` |
| 채워진 바 | `blue-500` | `blue-500` |
| 높이 | `1.5px` (`h-1.5`) | 동일 |
| 모서리 | `rounded-full` | 동일 |

### 애니메이션

```css
transition-all duration-500 ease-out
```

진행률 변경 시 0.5초 동안 부드럽게 전환

---

## 6. 타이밍 정리

| 애니메이션 | 지속 시간 | 이징 |
|-----------|----------|------|
| 카드 날아가기 | `500ms` | `ease-in-out` |
| 스케일 인 | `500ms` | `ease-out` |
| 바운스 인 | `500ms` | `ease-out` |
| 콘페티 | `3000ms` | `ease-in-out` |
| 진행 바 | `500ms` | `ease-out` |
| 버튼 전환 | `300ms` | default |

---

## 7. 반응형 고려사항

- 최대 너비: `max-w-sm` (384px)
- 패딩: `px-6 py-8`
- 풀스크린 레이아웃 (`fixed inset-0`)
- 세로 중앙 정렬 (`flex items-center justify-center`)

---

## 8. 다크 모드 대응

모든 컴포넌트는 `dark:` 프리픽스로 다크 모드 스타일 정의

```jsx
// 예시
className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white"
```
